<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication" >

    <uses-permission android:name="android.permission.BROADCAST_STICKY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication" >

        <activity
            android:name=".activity.MyViewGroupActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CustomActivity"
            android:exported="false" />

        <provider
            android:name=".contentProvider.MyContentProvider"
            android:authorities="com.example.myapplication.q"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.contentResolverActivity"
            android:exported="false" />
        <activity
            android:name=".activity.aidlActivity"
            android:exported="false" />

        <service
            android:name=".service.aidlService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.BActivity"
            android:exported="false" /> <!-- 静态注册广播，并设置监听内容 -->
        <receiver
            android:name=".broadCastReceive.myBroadCast"
            android:exported="true" >
            <intent-filter android:priority="100" >
                <action android:name="msg" />
            </intent-filter>
        </receiver> <!-- provider -->
        <activity
            android:name=".activity.AActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>